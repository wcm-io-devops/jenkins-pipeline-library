- patterns:
    - .*
  id: "default mqtt notification configuration"
  config:
    broker: defaultbroker

- patterns:
    - team-a\/.*
  id: "default mattermost notification configuration"
  config:
    broker: team-a-broker
    credentialsId: team-a-broker-credential-id
    qos: "2"
    retain: true
